{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "concurrently --kill-others --names \"vite,artisan\" --prefix-colors \"cyan,green\" \"vite\" \"php artisan serve\"",
        "build": "vite build",
        "serve": "php artisan serve",
        "vite": "vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.3.0",
        "concurrently": "^9.2.1",
        "laravel-vite-plugin": "^3.1",
        "tailwindcss": "^4.3.0",
        "vite": "^8.0.0"
    }
}